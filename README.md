A website for my wife's ADHD coaching service

`npx nuxi build --preset=cloudflare_pages`
`npx wrangler pages dev dist` - for local dev
`npx wrangler pages deploy dist`

or use the alias `npm run cloudflare-deploy` 